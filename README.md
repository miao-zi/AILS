# AILS
In this paper, we propose a surprisingly efficient implicit augmentation-invariant learning strategy (AILS) via variational Bayesian inference on differentially constrained feature manifolds.
The code is currently undergoing comprehensive collation and will be fully released
